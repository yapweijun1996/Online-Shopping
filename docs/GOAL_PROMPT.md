# Autonomous delivery contract

> Continue the Online Shopping MVP defined by GOAL, SPEC, DESIGN, API, EPIC, ROADMAP, TASK, PROGRESS, DEPLOY, PWA_I18N, and UI_SPEC. AC-01–15 and AC-18 are locally verified; AC-16/17 and release are open. `sample/` is a separate, untracked business-rule reference: never copy or modify its code, UI, or credentials. The two-container Docker design has Caddy serve separate shop/seller PWAs and proxy the documented API to one private Node/SQLite backend. Keep SQLite, hashed sessions, images and order PII in private persistent storage. The server owns auth, MYR prices, order numbers, snapshots, idempotency, revisions, decisions and audit. Use ignored server credentials; production rejects missing/weak values. Keep secrets and real PII out of source, logs and public artifacts. Support +60/+65 buyer and recipient phones, bounded base64 seller images, seven ordered UI languages, and opt-in same-browser clearable phone/address combobox history. Server order contacts have no automatic deletion; do not claim indefinite retention is lawful without an owner-approved basis/schedule. No shipping charges or logistics, GST, payment, customer accounts/OTP, cross-device recovery, or automatic messaging. Resolve the real HTTPS host, retention, backup/restore and rollback before real-data release. Loop: inspect current rules/code/tests and TASK/PROGRESS -> choose the highest-value unblocked task -> implement the smallest complete slice -> run API/data/build/browser/PWA/security checks appropriate to it -> self-review and independent review when useful -> reproduce and fix valid findings -> update contracts, README, TASK and PROGRESS -> make one focused verified local commit -> repeat. Track Planned, Implemented, Verified and Released separately; local tests never prove release. Preserve unrelated work. Never push, create a PR, merge, or publish without an explicit request.
