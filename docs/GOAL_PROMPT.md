# Goal prompt

> Build the Online Shopping MVP described by GOAL, SPEC, DESIGN, API, EPIC, ROADMAP, TASK, PROGRESS, PWA_I18N, and UI_SPEC. AC-01–14 run and are locally verified; finish AC-15/17/18 quality gates next, starting with seller product error/keyboard states, seven-language shop/seller journeys, and target-browser PWA install/update. AC-16 needs production hosting, session durability, backup/recovery, and contact-data retention decisions. `sample/` is only a business-rule reference; do not copy its code, UI, or credentials. Keep both PWAs dependent on the backend API and private SQLite database. The server owns authorization, MYR prices, order numbers, snapshots, idempotency, revisions, decisions, and audit. Use ignored server-side credentials; reject missing or weak production values. Keep real PII/secrets out of source, logs, and public artifacts. Support +60/+65 buyer/recipient phones and seven ordered UI languages. Use bounded base64 seller images with private decoded storage. Offer opt-in same-browser phone/address combobox history with typing, selection, and clearing; no public lookup. Collect destinations without shipping charges or eligibility rules. Defer GST, payment, customer accounts/OTP, cross-device recovery, automatic messaging, and logistics. Ask only when a material choice cannot be resolved from evidence; continue safe work. Loop: inspect rules/code/tests and TASK/PROGRESS -> choose highest-value unblocked task -> implement the smallest complete slice -> run relevant API/data/build/browser/PWA/i18n/security checks -> self-review and independent review when useful -> fix valid findings -> update TASK/PROGRESS/contracts/README -> make one focused verified local commit -> repeat. Track Planned, Implemented, Verified, and Released separately; local tests never prove release. Preserve unrelated work; never push, open PR, merge, or publish without an explicit request.
