# Goal prompt

The project-specific autonomous contract below must stay at or below 2,000 characters.

> Build the planned new Online Shopping MVP in docs/GOAL.md, SPEC.md, DESIGN.md, API.md, EPIC.md, ROADMAP.md, TASK.md, PROGRESS.md, PWA_I18N.md, and UI_SPEC.md. No new app exists. `sample/` is a separate local prototype for business-rule reference only; do not copy its UI, code, or demo credentials. First deliver seller sign-in and product management; then public catalog/cart, guest multi-destination checkout, and seller order review. Keep customer and seller PWAs separate from the API and private database. The server owns authorization, pricing, order numbers, snapshots, idempotency, revisions, decisions, and audit. Use ignored server-side initial credentials; reject missing or weak production values. Keep real PII and secrets out of source, logs, and public artifacts. Support +60/+65 phones, seven UI languages in the required order, install/offline shells, and online-only mutations. Defer GST, payment, accounts/OTP, cross-device recovery, automatic messaging, and logistics. Record open currency, shipping, delivery, image, hosting, session, and retention decisions; ask only for material choices evidence cannot resolve. Loop: inspect rules/code/tests and TASK/PROGRESS -> choose highest-value unblocked task -> implement smallest complete slice -> run relevant API/data/build/browser/PWA/i18n/security checks -> self-review and independent review when useful -> fix valid findings -> update TASK/PROGRESS/contracts/README -> make one focused verified local commit -> repeat. Track Planned, Implemented, Verified, and Released separately. Never claim release from local tests. Preserve unrelated work; never push, open PR, merge, or publish without explicit request.
