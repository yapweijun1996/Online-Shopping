# Executable task ledger

**Scope: the new Online Shopping MVP unless marked `SAMPLE`.** This file supersedes the former [TASKS.md](TASKS.md) checklist and retains its task IDs. `Recorded` means a planning decision exists, not that software implements it. `Verified` requires the stated evidence; `Released` requires deployed proof. Acceptance progress is counted from the 18 `AC` items in [SPEC.md](SPEC.md), not from task rows.

| ID | Priority / status | Depends on | Done condition and evidence needed |
| --- | --- | --- | --- |
| DOC-01 | P0 / Verified | — | Core goal, design, API, roadmap, task and progress documents describe the planned new app and separate `sample/`. Documentation audit and link checks. |
| DOC-02 | P0 / Verified | DOC-01 | [GOAL_PROMPT.md](GOAL_PROMPT.md) matches this project and is at most 2,000 characters; measured character count. |
| DEC-01 | P0 / Recorded | — | Seller-managed products and active-only public visibility are explicit in GOAL/SPEC/API; implement via API-03. |
| DEC-02 | P0 / Recorded | — | Shop uses MYR only. MVP does not calculate shipping charges or manage delivery eligibility/logistics. Checkout collects destination country, address and postcode for seller review without promising serviceability; no GST calculation. Server price and total remain authoritative. |
| DEC-03 | P0 / Recorded | — | One super admin is bootstrapped from ignored server-side `.env` or a Docker secret file, with no built-in production credentials; implemented via API-02. |
| DEC-04 | P0 / Partly recorded; open for real data | — | Owner chose Docker with two containers. Local Compose runs Caddy frontend and one Node API with SQLite/session hashes in a private persistent volume; no separate DB container. Confirm hostname, encrypted/off-host backup, recovery and rollback before real-data release. |
| DEC-05 | P1 / Recorded | — | Seller may send a bounded PNG/JPEG/WebP base64 data URL. Server validates format and size, stores decoded bytes in private SQLite, and serves active-product images from an API path; use a placeholder when absent. No external image dependency. |
| DEC-06 | P0 / Business request recorded; release blocked | — | Owner requires permanent retention of submitted order records, including contact names, phones and addresses. Current API has no order deletion path or automatic purge; an authorized DELETE attempt returns 404 and leaves all order rows intact in a regression test. This does not guarantee permanent recovery. Obtain the applicable lawful basis or statutory exception and an approved retention/disposal decision before real PII. Do not add a deletion job or an irreversible anti-deletion control while that decision is unresolved. Browser suggestion history remains separately clearable and expires after 90 days. |
| API-01 | P0 / Verified locally | DEC-04 local stack choice | New Node 24 API entry point and private SQLite schema are separate from `sample/`; health/readiness, private path/mode, and startup tests pass. Production operations remain OPS-01. |
| API-02 | P0 / Verified locally | API-01, DEC-03 | Admin login/session/logout, origin/CSRF checks, generic rate-limited failures, and product/order endpoint authorization tests pass. AC-01/02 are locally verified. |
| API-03 | P0 / Verified locally | API-01–02, DEC-02, DEC-05 | Seller create/edit/activate/deactivate and public active-only list/detail/search/category/image APIs use private schema v3. Authorization, CSRF, migration, duplicate SKU, image, active-only visibility and order snapshot regression tests pass; AC-03/04 are locally verified. Optional localized catalog content is LATER-02. |
| DEMO-01 | P2 / Verified locally; outside AC count | API-03, local Compose | A repeatable local-only seller API seed adds 20 fictional active MYR products with descriptions/details and generated WebP photos. Re-run skips all 20; public API returns 20 active products, six categories and 20 valid image responses; the in-app browser shows the populated catalog. |
| WEB-00 | P0 / Verified locally | API-02–03 | Seller login, responsive navigation/profile and product create/edit/availability/image form run locally. Desktop and 320/390px browser flows, seven-language product validation, duplicate-SKU field focus, and localized sign-in errors pass. |
| WEB-01 | P1 / Verified locally | API-03 | Public catalog search/detail and no-image placeholder use the active-only API. Desktop and 320/390px browser checks covered discovery, search, detail and unavailable behavior; product API tests cover active-only visibility. AC-04 is locally verified after the order API rejected unavailable products. |
| WEB-08 | P1 / Verified locally; outside AC count | WEB-01, DEMO-01 | The owner-supplied shop layout direction now uses real active-product data: responsive header search, banner, API category rail/select, denser cards and accessible detail. Local browser checks covered 320/390/1440px, seven-language header/card fit, search/filter/cart/checkout, dialog close, scroll top/middle/bottom and no measured overflow or console errors. Mock offers, ratings, accounts and shipping promises were excluded. |
| WEB-02 | P1 / Verified locally | WEB-01 | IndexedDB cart and page-memory fallback tests pass. Browser add/change/remove/reload, totals, unavailable feedback and confirmed checkout with failed browser storage writes pass; AC-05 is locally verified. |
| API-04 | P0 / Verified locally | API-03, DEC-02 | Guest order API validates +60/+65 buyer/recipient contacts, bounded destinations/assignments and consent; computes MYR totals without shipping/GST. API tests and multi-destination browser flow pass; AC-06–08 are locally verified. |
| API-05 | P0 / Verified locally | API-04 | Private sequence, order/delivery/item snapshots, consent, first event and hashed idempotency record commit atomically. Same-key replay, changed-key conflict, concurrent requests and injected persistence failure tests pass. AC-09 is verified; AC-10 is locally verified after the browser lost-response retry returned the original receipt and one stored order. |
| WEB-03 | P0 / Verified locally | WEB-02, API-04–05 | Guest checkout handles buyer and recipient +60/+65 phones, one or more destinations, item assignment, field errors, consent, a confirmed server receipt, lost-response retry and storage failure. Browser/API checks support AC-06–11 locally. |
| WEB-07 | P1 / Verified locally | WEB-03 | Explicitly saved same-browser buyer/recipient phones and addresses support typing, click and keyboard selection; clearing removes stored suggestions. Unit tests cover reload, expiry 90 days after the most recent save, malformed data and storage failure; no public lookup or service-worker cache is used. |
| API-06 | P0 / Verified locally | API-05 | Seller queue/detail, revision-fenced confirm/reject and append-only audit APIs pass authorization, pagination, state, stale/concurrent decision, and rollback tests. AC-12–13 seller UI/browser checks passed under WEB-04. |
| API-07 | P0 / Verified locally | API-02–06 | Invalid input, duplicate retries, concurrent decisions, access control, no-delete order API and persistence failure have targeted tests; 37/37 new-app tests pass on Node 24, including secret-file configuration. Browser stale-decision and offline recovery checks passed under WEB-04/QA-01. |
| WEB-04 | P0 / Verified locally | API-06 | `Sales Orders` all-status search/filter/detail and `Sales Order Confirmation` pending queue, explicit confirm/reject dialog and stale reload use one backend order state. Desktop/320px browser decisions and two-tab stale recovery pass; AC-12/13 are locally verified. |
| WEB-05 | P1 / Verified locally | WEB-04, AC-06 consent | Seller-only `wa.me` link appears for opted-in buyer, uses normalized digits without a prefilled message, and is absent otherwise. Browser URL/visibility and official format checks pass. |
| WEB-06 | P1 / Verified locally | WEB-04 | Buyer/recipient names, phones, address lines and postcode have individual copy controls. Browser clipboard success and forced failure produced inline and announced feedback; authorized detail only. AC-14 is locally verified. |
| PWA-01 | P0 / In progress | WEB-00–04 | Seller and shop have separate manifests/scopes, 192/512 icons and versioned public-shell caches. Offline seller review buttons disable and a failed offline decision never claims success; cache inspection showed public static assets only. Persistent desktop Chrome reported zero installability errors and emitted `beforeinstallprompt` for both local-loopback surfaces; actual installation, HTTPS deployment and target-device update remain unverified for AC-17. |
| I18N-01 | P0 / Verified locally | WEB-00–04 | Ordered seven-language resources, HTML language tags, local preference and locale-aware dates/prices pass resource parity and 320/1280px browser checks across seller/product/order and shop/catalog/cart/checkout/receipt. Switching preserves entered checkout/order data; seller navigation, sign-in/product/decision errors and dialog focus are localized. Seller-authored catalog text remains English; optional translated product content is separate API-03 work. AC-18 is locally verified. |
| QA-01 | P0 / In progress | WEB-00–06, API-07, PWA-01, I18N-01 | AC-15 and AC-18 passed local Chromium flows at 320/390/1280px, including loading/empty/error/success, keyboard/focus, labels and no observed horizontal overflow. Language-switch dialog focus and error-message defects were corrected and retested. Target-device PWA installation/update remains AC-17 under PWA-01. |
| QA-02 | P0 / Verified locally | API-07, QA-01 | Frontend/API flows and local sensitive-data boundary reviewed: no public contact lookup, private source/`.env` paths return 404, unauthenticated seller orders return 401 with `no-store`, and both worker caches contain public files only. Production artifact and real-data operations remain REL-01/OPS-01. |
| OPS-01 | P0 / In progress | DEC-04, API-01 | A Node 24 workflow is committed for isolated new-app syntax/tests and runtime dependency audit, with read-only GitHub token permissions and SHA-pinned actions; remote run unverified. Two pinned-base Docker images and Compose topology built locally. Proxy routes, private volume, nonroot secret/DB modes, restart persistence, spoof-resistant Caddy client-IP forwarding, a `0600` backup helper, and isolated synthetic-data restore passed. Complete lawful retention, encrypted/off-host production backup and restore targets, HTTPS health/readiness, logs and rollback against a release candidate. |
| REL-01 | P0 / In progress | QA-01–02, OPS-01, AC-16 | A local `git archive HEAD` inventory found no private database, `.env`, key, log or public PII signals; the single tracked `sample/docs/MVP_DOD.md` is historical documentation. Local Docker image inspection found only public assets in frontend and no public/sample tree in backend. Inspect the actual production artifact, then verify deployed version, health and rollback before marking any feature Released. No push or release is authorized. |
| DOC-03 | P1 / In progress | Implemented milestones | Keep README, design, API, SPEC, TASK and PROGRESS aligned with actual implementation, run commands and changed contracts. |
| SAMPLE-01 | P2 / Open, sample only | — | Repair or update the stale `sample/test/fulfillment.test.js` revision-snapshot assertion; rerun Node 24 suite. Current result: 31/32 pass, expected 2 rows but current append-only revisions produce 4. This is not an MVP acceptance item. |
| LATER-01 | Deferred | MVP complete; verified external format | Verify Ninja Van import requirements before proposing any CSV/Excel export or courier integration. |
| LATER-02 | Deferred | MVP complete; seller content need | Add optional translated product name/description with English fallback and stable order snapshots if sellers need localized catalog content. Current seven-language UI displays seller-authored product text. |

## Resume point

Resolve DEC-06 lawful basis for the requested permanent order retention and a real HTTPS hostname. Then define encrypted/off-host backup and rollback, verify target-browser AC-17 install/update, and run AC-16 production health/version/security gates. AC-15/18 and the local QA-02/Compose boundary checks passed; production artifact inspection remains REL-01. Do not copy sample credentials, customer access links, logistics workflow, or UI into the new app.
