# Progress

**As of 2026-09-25 (Asia/Singapore).** The new Online Shopping application is an **MVP in local development**. The separate `sample/` is a local PWA/API/SQLite prototype. Its functionality and test results do not count toward the new MVP.

## Evidence-based state

The denominator is the **18 independent AC-01–18 acceptance items** in [SPEC.md](SPEC.md). A planned design is not implementation; code without passing applicable checks is not verified; a local result is not released.

| New MVP state | Count | Basis |
| --- | ---: | --- |
| Planned | 18/18 | Requirements and acceptance gates recorded in SPEC, including PWA and seven-language UI. |
| Implemented | 16/18 | AC-01–15 and AC-18 have runnable local paths. Production operations and target-browser PWA installation remain. |
| Verified | 16/18 | AC-01–15 and AC-18 passed applicable local API/data/browser gates, including responsive keyboard/error states and seven-language journeys. This is local proof only. |
| Released | 0/18 | Local Docker images and a loopback Compose smoke exist, but no public HTTPS deployment, registry/tag/release, or deployed version/rollback proof exists. |

Documentation milestone **M0: 1/5 milestones verified** after the initial documentation audit and local commit. M1–M3 functional slices run locally but their milestone exit gates remain in progress; M4 local quality gates are in progress. This milestone count is separate from the 16/18 locally verified acceptance count.

## Current development slice

| Check | Evidence and limit |
| --- | --- |
| Runtime/config | Node 24.20.0 syntax checks pass. The new-app scoped suite has 36/36 `node:test` checks passing, including secret-file config, schema v1→v3 and v2→v3 migrations, product authorization/CSRF, active-only visibility, base64 image boundary, guest order atomicity/snapshots/validation, idempotency, concurrent requests and injected persistence failure. A root-wide `node --test` also collected the separate sample suite and reproduced its known 31/32 result; the project test script targets only `test/`. `npm audit --omit=dev` reported zero vulnerabilities for the one pinned runtime dependency. |
| Browser | Chromium seller login, reload persistence, account/profile, mobile drawer/Escape/focus return, and logout worked at 390px and desktop widths. Seller product create/edit/image preview and save worked; 320/390px product validation, duplicate-SKU message/focus, active toggle and seven-language error text passed. Public catalog search/detail, no-image placeholder, cart add/change/remove/reload, unavailable-line feedback and empty-search recovery worked at desktop and 320/390px widths. Checkout submitted two destinations with independent +60/+65 recipients and assigned products, returned a server receipt, and persisted the minimal receipt across reload. Typed/clicked/keyboard history selection, clear/reload, field-error focus, lost-response retry with one database order, and confirmed order despite failed browser storage writes passed. Seller order queue/filter/search/detail, two destinations, confirm/reject/audit, empty-search recovery and two-tab stale 409 reload passed. Copy success/failure, opted-in WhatsApp link, and sign-out removal of private detail DOM passed. At 320/1280px, seven-language seller/order and shop/catalog/cart/checkout/receipt screens showed translated titles, errors, status, accessible names and locale dates without measured horizontal overflow; switching preserved entered checkout data and order details. A dialog focus loss after language switching was fixed; cancel/Escape now return focus to the current action. Expected console entries came from deliberately exercised 401, 404, 409 and offline requests. AC-15/18 are locally verified; target-device PWA installation remains. |
| Security boundary | No credential is built into app source or images. Production configuration rejects missing, short and known-placeholder passwords; Compose mounts an ignored secret file. Admin password is scrypt-hashed; server sessions use hashed tokens in private SQLite, `HttpOnly`/`SameSite=Strict` cookies, same-origin mutation checks and CSRF. The local two-container topology keeps one backend process and its rate limiter behind Caddy; real-host HTTPS, backup, retention and deployment security remain unverified. |
| Docker runtime | Local Compose built pinned-base Caddy and Node images and started two containers on loopback. Through Caddy, `/ready`, both PWA shells/manifests/workers, public/seller API authorization, product create/visibility, one synthetic checkout and idempotent replay passed. The backend has no host port; its secret and SQLite file were mode `0600` under the nonroot user. Frontend image contained public assets only; backend image excluded public/sample files. One product and order persisted across backend restart. Spoofed inbound `X-Real-IP` values did not evade the login limiter. Missing/weak production credentials caused status-1 startup. The QA containers were stopped without removing their volume; a separate clean default Compose project now runs at `127.0.0.1:8080`, where empty catalog, seller login, both PWA pages and readiness passed. This is local smoke, not production release proof. |
| PWA shell | Local Chromium registered separate `/seller/` and `/shop/` workers. Current `os-seller-v27` and `os-shop-v22` caches contained only listed static assets and no `/api/` entries; both surfaces showed offline pages without private order detail. An offline seller decision showed no success; review buttons disable offline and reenable on reconnect. Worker updates waited while old pages were controlled, then activated after navigation and removed old caches in an earlier shell check. Manifest PNG dimensions passed a file-level check. A separate persistent desktop Chrome profile reported zero `Page.getInstallabilityErrors` and observed `beforeinstallprompt` on both local-loopback surfaces; an incognito test profile reported only `in-incognito`. Actual install, HTTPS deployment and target-device update remain unverified. |
| Cart storage | Local Chromium added, changed, removed and reloaded real product cart lines; the advisory total updated. Deactivating a product changed its cart row to unavailable and hid its price. Tests confirm page-memory behavior when IndexedDB is unavailable or opening throws. A browser checkout succeeded when receipt and cart storage writes were forced to fail; the receipt showed both storage warnings and the server order number. AC-05 is locally verified. |
| Contact validation | Server helpers accept structurally valid +60/+65 numbers and return E.164, normalize bounded buyer/recipient names, check optional email, require a boolean WhatsApp contact choice, and identify invalid fields without echoing values. Tests reject unsupported countries, short numbers, extensions, malformed input and control characters. The browser checkout uses the API, highlights invalid buyer/recipient fields, and handles +60/+65 independently of address country. Explicit same-browser history passed typed/clicked/keyboard selection, reload and clear tests; storage-failure/expiry cases passed unit tests. AC-06–07 are locally verified. Number validity does not establish reachability or ownership. |
| Product management | Private SQLite v3 and seller API/editor create, edit and change active status for MYR products. Base64 PNG/JPEG/WebP input is bounded and decoded into the private database; inactive product and image paths return 404 publicly. Product API contract tests and a desktop browser flow pass. Order snapshot regression and unavailable-product checkout rejection pass. AC-03–04 are locally verified; optional translated product content is deferred as LATER-02. |
| Guest order API | Schema v3 stores private order, destination, item snapshot, consent time/version, initial event and hashed idempotency record in one transaction. MYR totals ignore client price/shipping/GST fields. Tests prove two destinations with independent +60/+65 contacts, unavailable/invalid inputs, same-key replay/conflict, concurrency, atomic rollback and safe retry. AC-08–10 are locally verified; a browser test dropped the first successful response, retried the same payload, received the original receipt and observed one stored order. |
| Seller order review | Authorized queue/detail return one server snapshot. Confirm/reject require same origin and CSRF, the current revision, and a bounded rejection reason; the transaction records actor/time/state in append-only audit. API tests covered pagination/search, unauthenticated access, stale and simultaneous decisions, and an injected audit failure that rolled back state. Desktop/mobile UI consumed the same endpoints; browser checks covered search/status filters, detail, confirm/reject, stale reload, clipboard feedback and opted-in WhatsApp URL. AC-12–14 are locally verified. |
| Local QA/security | Node 24 syntax checks and 36/36 new-app tests pass; the runtime dependency audit reports zero vulnerabilities. Public `/src/server.js` and `/.env` return 404; unauthenticated seller orders return 401 with `Cache-Control: no-store`. Source review found no raw PII logging or dynamic HTML insertion of customer values. Both worker caches were inspected with no API entries. A `git archive HEAD` inventory found no private database, `.env`, key, log or public PII signals; it includes one tracked historical `sample/docs/MVP_DOD.md`, which is not new-app evidence. Docker image boundaries also passed local inspection. These local checks support QA-02; actual production artifact inspection remains. |
| CI configuration | `.github/workflows/verify.yml` selects Node 24, `npm ci`, syntax checks, the scoped new-app suite, and a high-severity runtime dependency audit. Checkout/setup-node are SHA-pinned and the token is read-only. YAML and equivalent local commands were checked; no GitHub Actions run exists yet. This is workflow configuration evidence, not a remote CI result or release proof. |

## Verification performed in this pass

| Check | Result and limit |
| --- | --- |
| Repository/rules/history | Inspected README, all first-party docs, source entry points/services, manifest, tests, ignore files, and Git state/history. No repository AGENTS.md, CLAUDE.md, CONTRIBUTING.md, CI/release workflow, or tag was present. Only `.gitattributes` was tracked at audit start; `.gitignore`, README, docs, and `sample/` were untracked user work. |
| KB-MCP | Live status was healthy. Recall returned one related project-focus memory; unrelated results were not treated as repository facts. Current code and explicit local decisions remain authoritative. |
| Prototype tests | `sample/`: `npx --yes node@24 --test` on Node 24.20.0 ran 32 tests: 31 pass, 1 fail. `fulfillment.test.js:25` expects 2 delivery rows across the order but confirmation copies two rows into revision 2, so the unfiltered query sees 4. The later assertion also expects source revision 1 despite current confirmation revision 2; recheck after repairing the test. This is `SAMPLE-01`, not a new-MVP failure. |
| Prototype runtime/API | Started the sample with an in-memory SQLite database: `/health` returned 200 and an unauthenticated `/api/seller/me` returned 401. This does not prove production readiness or new-app behavior. |
| Prototype browser | At 390px width, viewed shop catalog/detail/cart and seller login/products. Cart add feedback appeared; checkout stayed disabled without an address. Observed no horizontal overflow or console warning/error on those visited states. This is a limited sample smoke, not a full checkout/order-review E2E or new-app UI verification. |
| Documentation | Checked 13 first-party Markdown files for relative links, 18 ordered acceptance IDs, the 1,787-character goal prompt, both PNG preview headers/dimensions, and the final documentation diff. These checks verify document consistency, not product behavior. |

## Open decisions, risks, and next work

- **DEC-04:** the owner selected two Docker containers: Caddy frontend and one private Node API/SQLite backend with persistent volumes. Local loopback smoke passed. The exact HTTPS host, real-data retention basis/schedule, backup/recovery, and rollback remain unresolved. No automatic deletion of submitted order phones/addresses exists; indefinite retention is not yet approved. Browser suggestion history remains separately clearable and expires after 90 days.
- **DEC-02/DEC-05 recorded:** shop currency is MYR. No shipping charge, delivery-area rule, GST calculation or logistics integration is included. Addresses are collected for seller review. Seller images use bounded base64 input with private decoded storage and active-only serving; no-image placeholder is used.
- **On-device history:** checkout offers explicit opt-in saving, typed/clicked/keyboard combobox selection, expiry 90 days after the most recent save, and clearing. This is local browser convenience only; no public contact lookup or cross-device recovery is added.
- **SAMPLE-01:** one prototype test is stale against revisioned append-only snapshots. No prototype source or test repair is included in this documentation pass.
- PWA shells, offline fallback, and seven-language checkout/seller resources exist. Local responsive, locale and focus checks support AC-15/18; actual installation across target browsers and deployed update rollout remain AC-17.
- The new-app CI workflow has not run remotely. A local Docker build/smoke exists; no target-device PWA install/update E2E, production migration/restore, secret scan, HTTPS deployment, release, or rollback verification exists yet. Local browser and Compose checks cannot fill those gaps.

**Resume point:** resolve DEC-04 real-data contact retention basis/schedule and HTTPS host; rehearse an isolated Docker backup/restore and rollback. Then verify AC-17 PWA install/update on target browsers and AC-16 deployed health/version, security artifact and operational gates. Record implementation, verification, and release evidence per AC row as work proceeds.
