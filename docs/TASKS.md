# Task ledger redirect

The former checklist has been consolidated into [TASK.md](TASK.md), which retains its task IDs and adds priority, dependencies, done conditions, evidence, and the separate `SAMPLE-01` defect. Update `TASK.md` as the single task ledger for the new MVP.
